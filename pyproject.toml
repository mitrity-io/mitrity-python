[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "mitrity"
dynamic = ["version"]
description = "MITRITY governance adapter for Python agents: Claude Agent SDK, LangChain, OpenAI Agents SDK and CrewAI integrations for the MITRITY edge (admission API and gateway)."
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [{ name = "MITRITY AB", email = "hello@mitrity.com" }]
keywords = ["mitrity", "governance", "agents", "claude-agent-sdk", "langchain", "openai-agents", "crewai", "mcp"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Security",
  "Typing :: Typed",
]
dependencies = ["httpx>=0.27,<1"]

[project.optional-dependencies]
claude-agent-sdk = ["claude-agent-sdk>=0.2.157"]
langchain = ["langchain-core>=1.0"]
openai-agents = ["openai-agents>=0.20"]
crewai = ["crewai>=1.15"]
dev = [
  "anyio>=4.4",
  "claude-agent-sdk>=0.2.157",
  "crewai>=1.15",
  "langchain-core>=1.0",
  "openai-agents>=0.20",
  "mypy>=1.14",
  "pytest>=8.4",
  "ruff>=0.8",
]

[project.urls]
Homepage = "https://mitrity.com"
Source = "https://github.com/mitrity-io/mitrity-python"
Contract = "https://mitrity.com/docs/integrations/adapters"

[tool.hatch.version]
path = "src/mitrity/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/mitrity"]

[tool.hatch.build.targets.sdist]
include = ["src/mitrity", "tests", "README.md", "LICENSE", "SECURITY.md"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM", "N", "RUF", "PL", "T20", "S"]
ignore = [
  "PLR0913", # explicit keyword arguments are the public API
  "PLR2004", # magic values in tests and wire constants
  "S101",    # assert is how tests assert
  "S603",    # subprocess without shell, deliberate in tests
  "S108",    # /tmp paths in tests
  "S105",    # `token_file` is a path to a token, not a token
  "S106",    # same, as a keyword argument
  "N818",    # the exception names are the contract's (AdmissionUnreachable, AdmissionTimeout, ...)
  "RUF001",  # prose and test vectors use dashes and non-ASCII on purpose
  "RUF002",
  "RUF003",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["PLR0915", "PLC0415", "S104", "SIM117"]
".github/scripts/**" = ["T20"] # release_check.py reports to the console on purpose

[tool.ruff.lint.isort]
known-first-party = ["mitrity"]

[tool.mypy]
# 3.12, not the 3.10 floor: crewai's dependency chain reaches numpy, whose inline
# stubs use PEP 695 `type` statements that mypy refuses under an older target, and
# mypy 2.x parses an installed package's own stubs regardless of follow_imports.
# The 3.10 floor is still enforced by ruff (target-version py310) and by the CI
# test job that imports every module on 3.10.
python_version = "3.12"
strict = true
warn_unreachable = true
show_error_codes = true
files = ["src", "tests"]

[[tool.mypy.overrides]]
module = ["claude_agent_sdk.*", "langchain_core.*", "agents.*", "crewai.*", "openai.*"]
follow_untyped_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q -ra"
filterwarnings = ["error::DeprecationWarning:mitrity.*"]
